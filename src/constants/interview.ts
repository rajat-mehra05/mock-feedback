export const RETRY_MAX_ATTEMPTS = 3;
export const RETRY_BASE_DELAY_MS = 1_000;
export const RETRY_MAX_DELAY_MS = 4_000;

// Time to display question text when TTS fails (ms)
export const TTS_FALLBACK_DISPLAY_MS = 3_000;
