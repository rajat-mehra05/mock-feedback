{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions for the main window. Expanded in Phase 7 when Rust commands are introduced.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-show"]
}
